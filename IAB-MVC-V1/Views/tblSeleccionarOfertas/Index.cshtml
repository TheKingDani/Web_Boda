@model IEnumerable<IAB_MVC_V1.Models.tblSeleccionarOfertas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tblOfertasProveedores.idOfertaProveedor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblProveedores.nomNegocio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tblOfertasProveedores.idOfertaProveedor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblProveedores.nomNegocio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idSeleccionOferta }) |
            @Html.ActionLink("Details", "Details", new { id=item.idSeleccionOferta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idSeleccionOferta })
        </td>
    </tr>
}

</table>
