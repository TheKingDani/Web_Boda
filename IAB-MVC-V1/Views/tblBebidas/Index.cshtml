@model IEnumerable<IAB_MVC_V1.Models.tblBebidas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.bebida)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.bebida)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idBebida }) |
            @Html.ActionLink("Details", "Details", new { id=item.idBebida }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idBebida })
        </td>
    </tr>
}

</table>
