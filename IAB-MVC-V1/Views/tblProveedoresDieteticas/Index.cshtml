@model IEnumerable<IAB_MVC_V1.Models.tblProveedoresDieteticas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tblDieteticas.nomDietetica)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblProveedores.nomNegocio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tblDieteticas.nomDietetica)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblProveedores.nomNegocio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idProveedorDietetica }) |
            @Html.ActionLink("Details", "Details", new { id=item.idProveedorDietetica }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idProveedorDietetica })
        </td>
    </tr>
}

</table>
