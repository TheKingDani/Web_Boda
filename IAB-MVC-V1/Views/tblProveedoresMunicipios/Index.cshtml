@model IEnumerable<IAB_MVC_V1.Models.tblProveedoresMunicipios>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tblMunicipios.nombreMunicipio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblProveedores.nomNegocio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tblMunicipios.nombreMunicipio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblProveedores.nomNegocio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idProveedorMunicipio }) |
            @Html.ActionLink("Details", "Details", new { id=item.idProveedorMunicipio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idProveedorMunicipio })
        </td>
    </tr>
}

</table>
