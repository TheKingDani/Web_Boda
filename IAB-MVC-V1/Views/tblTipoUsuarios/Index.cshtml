@model IEnumerable<IAB_MVC_V1.Models.tblTipoUsuarios>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombreTipoUsuario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreTipoUsuario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.tipoUsuarioID }) |
            @Html.ActionLink("Details", "Details", new { id=item.tipoUsuarioID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.tipoUsuarioID })
        </td>
    </tr>
}

</table>
