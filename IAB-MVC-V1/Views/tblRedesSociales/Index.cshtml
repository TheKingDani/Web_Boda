@model IEnumerable<IAB_MVC_V1.Models.tblRedesSociales>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.redSocial)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.redSocial)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idRedSocial }) |
            @Html.ActionLink("Details", "Details", new { id=item.idRedSocial }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idRedSocial })
        </td>
    </tr>
}

</table>
