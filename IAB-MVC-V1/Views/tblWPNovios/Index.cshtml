@model IEnumerable<IAB_MVC_V1.Models.tblWPNovios>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tblWeddingPlanners.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblNovios.nombreNovia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tblWeddingPlanners.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblNovios.nombreNovia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idWPNovios }) |
            @Html.ActionLink("Details", "Details", new { id=item.idWPNovios }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idWPNovios })
        </td>
    </tr>
}

</table>
