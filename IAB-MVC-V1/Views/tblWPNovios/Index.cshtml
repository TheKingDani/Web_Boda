@model IEnumerable<IAB_MVC_V1.Models.tblWPNovios>

@{
    ViewBag.Title = "Organizador de Novios";
}

<h2>Novios</h2>

<p>
    @Html.ActionLink("Registrar", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Orgnizador
        </th>
        <th>
           Novios
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tblWeddingPlanners.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblNovios.nombreNovia)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.idWPNovios }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.idWPNovios }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.idWPNovios })
        </td>
    </tr>
}

</table>
